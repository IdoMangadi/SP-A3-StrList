#include <stdio.h>
#include "StrList.h"

int main(){
    int choice = 0;
    while(scanf(" %d", &choice) == 0){}

    while(choice != 0){
        if (choice == 1){
            char a;
            scanf(" %d", &a);
            if (a == 'A'){
                scanf()
            }
        }

    }
    return 0;
}